import {useWhiteboardStore} from "../../store/whiteboard";
import Konva from "konva";


const {stage} = useWhiteboardStore();

const onPanMouseUp = () => {
  stage.value.draggable(false)

}

const onPanMouseMove = (x, y) => {


}

const onPanMouseDown = () => {
  // set stage to draggable
  stage.value.draggable(true)
  console.log('draggable');

}

export default () => {
  return {
    onPanMouseDown,
    onPanMouseUp,
    onPanMouseMove,
  }
}
